IF(MONTH( ***Date to validate*** ) < 7, IF(TODAY() > DATEVALUE( TEXT(YEAR(TODAY())) & "-06-30" ), "P", "C"), IF(TODAY() < DATEVALUE( TEXT(YEAR(TODAY())) & "-06-30" ), "P", "C"))
// P equals prior year; C equals current year
